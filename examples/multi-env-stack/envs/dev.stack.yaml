$schema: ../../../.vscode/schemas/stack.schema.json

metadata:
  owner: platform-team
  cost_center: development

services:
  api:
    enabled: true
    port: 8080
    tier: frontend
  worker:
    enabled: true
    port: 9090
    tier: backend
    upstream_service_id: "${service.api.service_id}"
