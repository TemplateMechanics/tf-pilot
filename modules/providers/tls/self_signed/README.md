# tls/self_signed module

Reflection-scaffolded module contract for the tls self_signed capability family.

Mapped reflection prefixes:
- Resource types: tls_self_signed_cert, tls_locally_signed_cert
- Data source types: tls_public_key

This scaffold is intentionally safe by default and intended for incremental expansion.
