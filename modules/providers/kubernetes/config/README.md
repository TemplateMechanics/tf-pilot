# kubernetes/config module

Reflection-scaffolded module contract for the kubernetes config capability family.

Mapped reflection prefixes:
- Resource types: kubernetes_config_map, kubernetes_secret
- Data source types: kubernetes_config_map, kubernetes_secret

This scaffold is intentionally safe by default and intended for incremental expansion.
