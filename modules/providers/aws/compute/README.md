# aws/compute module

Reflection-scaffolded module contract for the aws compute capability family.

Mapped reflection prefixes:
- Resource types: aws_instance, aws_launch_template, aws_ecs_, aws_autoscaling_
- Data source types: aws_ami, aws_ecs_, aws_launch_template

This scaffold is intentionally safe by default and intended for incremental expansion.
